enum StatusCodes {
  OK = 200,
  CREATED = 201,
  SUCCESS_NO_RESPONSE = 204,
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  NOT_FOUND = 404,
  SEMANTIC_ERROR = 422,
  SERVER_ERROR = 500,
}

export default StatusCodes;
